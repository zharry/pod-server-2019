type: app

project:
  name: PodServer
  version: 0.0.1
  keywords:
  - wio
  - app
  compile_options:
    wio_version: 0.5.1
    default_target: main

targets:
   main:
    src: src
 platform: native

libraries:
  uWebSockets:
    lib_path: ./shared/uWebSockets/lib
    include_path: ./shared/uWebSockets/src
